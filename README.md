# novaos-agent-sdk

Java client library for connecting to and communicating with AI Agents that implement the Agent Communication Protocol (ACP).

Maven coordinates: `com.orbitronai:novaos-agent-sdk`
